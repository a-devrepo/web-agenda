diff --git a/src/app/pages/consultar-tarefa/consultar-tarefa.html b/src/app/pages/consultar-tarefa/consultar-tarefa.html
index 098d445..6e7aeeb 100644
--- a/src/app/pages/consultar-tarefa/consultar-tarefa.html
+++ b/src/app/pages/consultar-tarefa/consultar-tarefa.html
@@ -1,73 +1,88 @@
 <!-- MENU SUPERIOR -->
-<app-navbar/>
+<app-navbar />
 
 <div class="container-fluid">
   <div class="row">
+    <!-- MENU LATERAL -->
+    <div class="col-md-2 bg-light min-vh-100 pt-3">
+      <app-sidebar />
+    </div>
 
-      <!-- MENU LATERAL -->
-      <div class="col-md-2 bg-light min-vh-100 pt-3">
-        <app-sidebar/>
-      </div>
-
-      <!-- ÁREA CENTRAL -->
-      <div class="col-md-10 p-4">
-
-          <div class="card shadow">
-              <div class="card-body">
+    <!-- ÁREA CENTRAL -->
+    <div class="col-md-10 p-4">
+      <div class="card shadow">
+        <div class="card-body">
+          <h4>Consulta de tarefas</h4>
+          <p>Listagem de tarefas cadastradas em sua agenda</p>
 
-                <h4>Consulta de tarefas</h4>
-                <p>Listagem de tarefas cadastradas em sua agenda</p>
-
-                @if(mensagemSucesso()) {
-                  <div class="message-container">
-                    <div class="alert alert-success alert-dismissible fade show shadow" role="alert">
-                      <strong>{{ mensagemSucesso() }}</strong>
-                      <button type="button" class="btn-close" (click)="mensagemSucesso.set('')"></button>
-                    </div>
-                  </div>
-              } @else if(mensagemErro()) {
-                <div class="message-container">
-                  <div class="alert alert-danger alert-dismissible fade show shadow" role="alert">
-                    <strong>{{ mensagemErro() }}</strong>
-                    <button type="button" class="btn-close" (click)="mensagemErro.set('')"></button>
-                  </div>
-                </div>
-              }
+          @if (mensagemSucesso()) {
+            <div class="message-container">
+              <div class="alert alert-success alert-dismissible fade show shadow" role="alert">
+                <strong>{{ mensagemSucesso() }}</strong>
+                <button type="button" class="btn-close" (click)="mensagemSucesso.set('')"></button>
+              </div>
+            </div>
+          } @else if (mensagemErro()) {
+            <div class="message-container">
+              <div class="alert alert-danger alert-dismissible fade show shadow" role="alert">
+                <strong>{{ mensagemErro() }}</strong>
+                <button type="button" class="btn-close" (click)="mensagemErro.set('')"></button>
+              </div>
+            </div>
+          }
 
-                @if(tarefas().length == 0){
-                  <div class="alert  alert-warning">
-                    Não há tarefas cadastradas em sua agenda
-                  </div>
-                }@else if(tarefas().length > 0){
-                    <table class="table table-hover table-sm">
-                      <thead>
-                        <tr>
-                          <th>Nome da tarefa</th>
-                          <th>Ações</th>
-                        </tr>
-                      </thead>
-                      <tbody>
-                        @for(c of tarefas(); track c.id){
-                          <tr>
-                            <td>{{c.nome | uppercase }}</td>
-                            <button class="btn btn-outline-primary me-1" [routerLink]="['/pages/editar-tarefa', c.id]">Editar</button>
-                            <button class="btn btn-outline-danger" (click)="onDelete(c)">Excluir</button>
-                          </tr>
-                        }
-                      </tbody>
-                      <tfoot>
-                        <tr>
-                          <td colspan="2">
-                            Total de registros: {{tarefas().length}}
-                          </td>
-                        </tr>
-                      </tfoot>
-                    </table>
-                  }
+          <form [formGroup]="formulario" (ngSubmit)="consultar()">
+            <div class="row mb-3">
+              <div class="col-md-3">
+                <input type="date" class="form-control" formControlName="dataMin" />
+              </div>
+              <div class="col-md-3">
+                <input type="date" class="form-control" formControlName="dataMax" />
               </div>
-          </div>
+              <div class="col-md-6">
+                <input
+                  type="submit"
+                  class="btn btn-success"
+                  value="Pesquisar"
+                  [disabled]="formulario.invalid"
+                />
+              </div>
+            </div>
+          </form>
 
+          @if (tarefas().length == 0) {
+            <div class="alert alert-warning">Não há tarefas cadastradas em sua agenda</div>
+          } @else if (tarefas().length > 0) {
+            <table class="table table-hover table-sm">
+              <thead>
+                <tr>
+                  <th>Nome da tarefa</th>
+                  <th>Ações</th>
+                </tr>
+              </thead>
+              <tbody>
+                @for (c of tarefas(); track c.id) {
+                  <tr>
+                    <td>{{ c.nome | uppercase }}</td>
+                    <button
+                      class="btn btn-outline-primary me-1"
+                      [routerLink]="['/pages/editar-tarefa', c.id]"
+                    >
+                      Editar
+                    </button>
+                    <button class="btn btn-outline-danger" (click)="onDelete(c)">Excluir</button>
+                  </tr>
+                }
+              </tbody>
+              <tfoot>
+                <tr>
+                  <td colspan="2">Total de registros: {{ tarefas().length }}</td>
+                </tr>
+              </tfoot>
+            </table>
+          }
+        </div>
       </div>
-
+    </div>
   </div>
-</div>
\ No newline at end of file
+</div>
diff --git a/src/app/pages/consultar-tarefa/consultar-tarefa.ts b/src/app/pages/consultar-tarefa/consultar-tarefa.ts
index df0f5b5..3387476 100644
--- a/src/app/pages/consultar-tarefa/consultar-tarefa.ts
+++ b/src/app/pages/consultar-tarefa/consultar-tarefa.ts
@@ -1,28 +1,26 @@
 import { CommonModule } from '@angular/common';
 import { Component, inject, signal } from '@angular/core';
-import { FormsModule, ReactiveFormsModule } from '@angular/forms';
+import {
+  FormControl,
+  FormGroup,
+  FormsModule,
+  ReactiveFormsModule,
+  Validators,
+} from '@angular/forms';
 import { RouterLink } from '@angular/router';
 import { Sidebar } from '../../layout/sidebar/sidebar';
 import { Navbar } from '../../layout/navbar/navbar';
 import { HttpClient } from '@angular/common/http';
 import { ApiErrorResponse } from '../../interfaces/ApiErrorResponse';
+import de from '@angular/common/locales/de';
 
 @Component({
   selector: 'app-consultar-tarefa',
-  imports: [
-    CommonModule,
-    FormsModule,
-    ReactiveFormsModule,
-    RouterLink,
-    Sidebar,
-    Navbar
-  ],
+  imports: [CommonModule, FormsModule, ReactiveFormsModule, RouterLink, Sidebar, Navbar],
   templateUrl: './consultar-tarefa.html',
   styleUrl: './consultar-tarefa.css',
 })
 export class ConsultarTarefa {
-
-
   private httpClient = inject(HttpClient);
 
   tarefas = signal<any[]>([]);
@@ -30,47 +28,70 @@ export class ConsultarTarefa {
   mensagemSucesso = signal<string>('');
   errors = signal<ApiErrorResponse | null>(null);
 
-  apiBaseUrl = 'http://localhost:8082/api/v1/tarefas'
+  apiBaseUrl = 'http://localhost:8082/api/v1/tarefas';
 
+  formulario = new FormGroup({
+    dataMin: new FormControl('', [Validators.required]),
+    dataMax: new FormControl('', [Validators.required]),
+  });
 
   ngOnInit(): void {
+    this.carregarTarefas();
+
+    const hoje = new Date();
+    const primeiroDia = new Date(hoje.getFullYear(), hoje.getMonth(), 1);
+
+    const ultimoDia = new Date(hoje.getFullYear(), hoje.getMonth() + 1, 0);
+    const formatarData = (data: Date) => {
+      return data.toISOString().split('T')[0];
+    };
+    this.formulario.patchValue({
+      dataMin: formatarData(primeiroDia),
+      dataMax: formatarData(ultimoDia),
+    });
+    this.consultar();
+  }
 
-    this.httpClient.get(this.apiBaseUrl)
-    .subscribe({
-      next:(data) => {
+  carregarTarefas() {
+    this.httpClient.get(this.apiBaseUrl).subscribe({
+      next: (data) => {
         console.log(data);
         this.tarefas.set(data as any[]);
       },
       error: (err) => {
-        console.log(err.error)
+        console.log(err.error);
         this.errors.set(err.error as ApiErrorResponse);
-      }
-    })
+      },
+    });
   }
 
   onDelete(tarefa: any) {
-
     this.limparMensagens();
 
-    if(confirm('Deseja realmente excluir a tarefa: ' + tarefa.nome + '?')) {
-      //Enviando uma requisição do tipo DELETE para a API
-      this.httpClient.delete(this.apiBaseUrl+ '/' + tarefa.id)
-        .subscribe({
-          next: (data: any) => {
-            this.mensagemSucesso.set('Tarefa: ' + data.nome + ", excluída com sucesso!");
-            this.ngOnInit();
-          },
-          error: (e) => {
-            this.errors.set(e.error);
-          }
-        });
+    if (confirm('Deseja realmente excluir a tarefa: ' + tarefa.nome + '?')) {
+      this.httpClient.delete(this.apiBaseUrl + '/' + tarefa.id).subscribe({
+        next: (data: any) => {
+          this.mensagemSucesso.set('Tarefa: ' + data.nome + ', excluída com sucesso!');
+          this.ngOnInit();
+        },
+        error: (e) => {
+          this.errors.set(e.error);
+        },
+      });
     }
   }
 
+  consultar() {
+    const dataMin = this.formulario.value.dataMin;
+    const dataMax = this.formulario.value.dataMax;
+    this.httpClient.get(this.apiBaseUrl + '/' + dataMin + '/' + dataMax).subscribe((data) => {
+      this.tarefas.set(data as any[]);
+    });
+  }
+
   private limparMensagens() {
     this.mensagemSucesso = signal<string>('');
     this.errors.set(null);
     this.mensagemErro();
   }
-
 }
